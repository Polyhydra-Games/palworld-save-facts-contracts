name: Release contracts

on:
  workflow_dispatch:
    inputs:
      tag:
        description: Release tag (for example v0.2.0)
        required: true
        type: string

permissions:
  contents: write

jobs:
  package:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-dotnet@v5
        with:
          dotnet-version: 8.0.x
      - name: Test and pack
        run: |
          dotnet test PalworldSaveFacts.Contracts.sln -c Release
          dotnet pack PalworldSaveFacts.Contracts.sln -c Release -o artifacts
          sha256sum artifacts/*.nupkg > artifacts/SHA256SUMS.txt
      - uses: actions/upload-artifact@v4
        with:
          name: release-assets
          path: |
            artifacts/*.nupkg
            artifacts/SHA256SUMS.txt
            schema/*.schema.json
  publish:
    needs: package
    runs-on: ubuntu-latest
    environment: nuget-production
    steps:
      - uses: actions/download-artifact@v4
        with:
          name: release-assets
          path: release
      - name: Publish to NuGet.org
        env:
          NUGET_API_KEY: ${{ secrets.NUGET_API_KEY }}
        run: dotnet nuget push 'release/*.nupkg' --source https://api.nuget.org/v3/index.json --api-key "$NUGET_API_KEY" --skip-duplicate
      - uses: softprops/action-gh-release@v2
        with:
          tag_name: ${{ inputs.tag }}
          generate_release_notes: true
          files: release/**
